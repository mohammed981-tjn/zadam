name: zadam_delivery
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  firebase_auth: ^4.14.0
  cloud_firestore: ^4.13.0
  firebase_storage: ^11.5.0
  provider: ^6.0.0          # ✅ NOT 6.2.0
  google_fonts: ^6.1.0      # ✅ NOT 6.2.1
  flutter_rating_bar: ^4.0.1
  badges: ^3.1.0
  shimmer: ^3.0.0
  fl_chart: ^0.65.0
  uuid: ^4.0.0
  intl: ^0.19.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/