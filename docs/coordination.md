# لوح التنسيق — من يعمل على ماذا

> **اقرأ هذا الملف أولاً قبل أن تبدأ أي عمل على المستودع، وحدّثه قبل أن تنهي جلستك.**
> يخص كل من يعمل هنا: المالك، وأي مساعد ذكي، وأي مساهم.

## لماذا يوجد هذا الملف

في 19 أغسطس 2026 اكتُشف أن مراجعة أمنية كاملة — مع إصلاحات ومهاجرات واختبارات — بُنيت على
نسخة من المشروع عمرها أسبوع. وكان المالك خلال ذلك الأسبوع قد أصلح **نفس الثغرات** بنفسه:
تزوير سعر الحصة، وفحص السعة، واستخراج سياسات RLS إلى المستودع، ومشغّل منع تصعيد الصلاحيات.

لم يضع شيء من عمل المالك، لكن ضاع عمل المراجعة كله، ولم يكتشف أحد التعارض إلا بعد انتهائه.
**السبب الوحيد: لا أحد كان يعلم بما يفعله الآخر.** هذا الملف هو العلاج.

---

## ١. قواعد قصيرة

1. **قبل البدء:** اقرأ الجدول أدناه، ثم نفّذ `git fetch origin main` وتأكد أن فرعك ليس
   متأخراً. لو تأخّر أكثر من بضعة commits، أعِد بناءه على `main` قبل أن تكتب سطراً.
   ```bash
   git fetch origin main && git rev-list --count HEAD..origin/main
   # 0 = محدَّث · رقم كبير = توقّف وأعد البناء أولاً
   ```
2. **عند البدء:** أضف صفّاً لنفسك في «العمل الجاري» قبل أن تعمل، لا بعده.
3. **عند الانتهاء:** انقل صفّك إلى «السجل»، واذكر ما أُنجز وما لم يُنجز.
4. **لا تعمل على منطقة يحملها غيرك** في الجدول. إن اضطررت، اكتب ذلك في صفّه.
5. **`main` لا يُدفع إليه مباشرة.** كل عمل عبر فرع و PR.

---

## ٢. العمل الجاري

| المنطقة | مَن | الفرع | منذ | الحالة |
|---|---|---|---|---|
| العقود · مقدّمو الخدمة · المساعد | Claude | `claude/project-review-analysis-dt7lsy` | 2026-08-19 | إصلاحات المراجعة — بانتظار تطبيق الهجرات |

---

## ٣. المناطق ومالكوها

خريطة سريعة تمنع التصادم. «المالك» يعني: من يُسأل قبل التغيير، لا من يملك الحق وحده.

| المنطقة | الملفات |
|---|---|
| المصادقة والهوية | `src/app/login/`, `src/lib/phone*.ts`, `src/app/reset/`, `src/app/auth/` |
| المال والاستثمار | `src/app/projects/`, `src/lib/config.ts` (`INVESTMENT_LIVE`), `confirm_investment` |
| العقود والخدمات | `src/app/contracts/`, `src/app/services/`, `src/lib/services.ts` |
| المواسم والقطعان والأراضي | `src/app/seasons/`, `src/app/herds/`, `src/app/lands/` |
| المساعد الذكي | `src/app/api/assistant/`, `src/lib/{retrieval,embedding,engines,localAnswer,answerCache}.ts` |
| قاعدة البيانات | `supabase/migrations/` — **أي تغيير هنا يمسّ الجميع** |
| تطبيق الجوّال | `mobile/` |
| المراجع والبيانات | `src/lib/{reference,marketData,cropBenchmark,faostat}*`, `docs/` |

---

## ٤. السجل

بالأحدث أولاً.

### 2026-08-19 · إصلاح نتائج المراجعة · Claude

**الفرع:** `claude/project-review-analysis-dt7lsy` · **الحالة:** الكود مبنيّ ومفحوص،
**الهجرات لم تُطبَّق بعد**.

| ما أُصلح | أين |
|---|---|
| اعتماد المرحلة صار من صلاحية العميل أو الإدارة، لا مقدّم الخدمة (د-1) | `20260819180000_milestone_approval_actor.sql` |
| `active` انضم لحراسة التوثيق، فلا تُعيد الجهة الموقوفة تفعيل نفسها (د-2) | `20260819180100_guard_provider_active.sql` |
| سحب `EXECUTE` عن `check_assistant_rate_limit` + قلب الافتراض للدوال الجديدة (ع-1، ع-2) | `20260819180200_rate_limit_grants.sql` |
| تحديد المعدّل صار عبر `service_role` **ويفشل مغلقاً** (ع-3) | `src/lib/assistantRateLimit.ts` + `api/assistant/route.ts` |
| رسائل قاعدة البيانات الخام لم تعد تصل للمتصفح (م-1) | `api/assistant/route.ts` |
| مفتاح Gemini انتقل من الرابط إلى ترويسة `x-goog-api-key` (م-2) | `lib/engines.ts`, `lib/embedding.ts` (٣ مواضع) |
| حذف حقل السعر المخفي الميت من نموذج الاستثمار (م-3) | `projects/[slug]/page.tsx` |

**⚠️ ترتيب النشر إلزامي:** الهجرات أولاً ثم الكود. تحديداً `20260819180200` والتغيير في
مسار المساعد **يجب أن ينزلا معاً** — الملف يشرح السبب في رأسه.

**لم يُصلح بعد:** د-3 (رفض RLS لا يُميَّز عن النجاح — 9 مواضع) · د-4 (ثماني عمليات تتجاهل
النتيجة، منها قيد دفتر الحسابات) · ع-4 (أربع دوال وجدول خارج المستودع).

### 2026-08-19 · مراجعة أمنية على `0514ac1` · Claude

**الفرع:** `claude/project-review-analysis-dt7lsy` · **الحالة:** مكتمل، بانتظار المراجعة
· **التقرير:** [`docs/security-review-2026-08-19.md`](security-review-2026-08-19.md)

**فُحص:** الهجرات العشرون كاملة · مسارات API الخمسة · مسار المال · نموذج الأدوار ·
كل دوال الخادم الـ41 في 16 ملفاً.

**أهم ما وُجد:**
- مقدّم الخدمة يستطيع اعتماد مراحل عقده بنفسه وتسجيلها مدفوعة (د-1)
- الجهة الموقوفة تُعيد تفعيل نفسها (د-2)
- `check_assistant_rate_limit` قابلة للاستدعاء من أي زائر مع تمرير IP الضحية (ع-1)
- `revoke execute` مكتوبة مرة واحدة مقابل 19 دالة (ع-2)
- تحديد المعدّل يفشل مفتوحاً (ع-3) — **يجب إصلاحه مع ع-1 معاً**
- أربع دوال وجدول `assistant_requests` ما زالت خارج المستودع (ع-4)

**لم يُفحص:** رفع الملفات وبيانات EXIF · تطبيق الجوّال (79 ملف Flutter) ·
وحدات الخدمات والثروة الحيوانية و FAOSTAT ودراسة القناة كمنطق أعمال (فُحصت أمنياً فقط).

**لم يُطبَّق أي إصلاح** — التقرير توثيقي بالكامل، ولا يغيّر أي كود.

### 2026-08-12 → 08-19 · مراجعة أولى — أُلغيت · Claude

بُنيت على `8388259`، وتبيّن أنها متأخرة 97 commit. كل ما أصلحته كان مُصلَحاً على `main`
بالفعل، فأُلغيت واستُبدلت بالمراجعة أعلاه. الـ commits محفوظة عند `c0ce1dd` إن احتيج إليها.

**الدرس المستخلص:** هذا الملف.
