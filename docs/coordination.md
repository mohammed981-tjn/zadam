# لوح التنسيق — من يعمل على ماذا

> **اقرأ هذا الملف أولاً قبل أن تبدأ أي عمل على المستودع، وحدّثه قبل أن تنهي جلستك.**
> يخص كل من يعمل هنا: المالك، وأي مساعد ذكي، وأي مساهم.

## لماذا يوجد هذا الملف

في 19 أغسطس 2026 اكتُشف أن مراجعة أمنية كاملة — مع إصلاحات وهجرات واختبارات — بُنيت على
نسخة من المشروع عمرها أسبوع. وكان المالك خلال ذلك الأسبوع قد أصلح **نفس الثغرات** بنفسه:
تزوير سعر الحصة، وفحص السعة، واستخراج سياسات RLS إلى المستودع، ومشغّل منع تصعيد الصلاحيات.

لم يضع شيء من عمل المالك، لكن ضاع عمل المراجعة كله، ولم يكتشف أحد التعارض إلا بعد انتهائه.
**السبب الوحيد: لا أحد كان يعلم بما يفعله الآخر.** هذا الملف هو العلاج.

---

## ٠. كيف يُمنع التعارض فعلياً

هناك نوعان من التعارض، ولكلٍّ علاج مختلف — والخلط بينهما هو ما ضيّع الأسبوع:

| النوع | ما يحدث | مَن يمنعه |
|---|---|---|
| **تعارض الملفات** | طرفان يعدّلان نفس السطر | **Git يمنعه تلقائياً.** الدمج يتوقف ويطلب حلاً يدوياً — لا يضيع شيء بصمت |
| **تعارض الجهد** | طرفان يحلّان نفس المشكلة بطريقتين | **Git لا يراه إطلاقاً.** هذا الملف وحده يمنعه |

**كل طرف على فرعه المستقل، ولا أحد يدفع إلى `main` مباشرة.** فالنوع الأول محكوم بالأداة.
أما النوع الثاني — وهو الذي وقع فعلاً — فيُمنع بشيء واحد: **أن تكتب في جدول «العمل الجاري»
قبل أن تبدأ، لا بعد أن تنتهي.**

---

## ١. توزيع الأدوار

| | **المصحّح** (Claude — المراجعة) | **البنّاء** (Claude — الميزات) |
|---|---|---|
| **الفرع** | `claude/project-review-analysis-dt7lsy` — واحد طويل | `claude/<feature>` — قصيرة، واحد لكل ميزة |
| **المهمة** | مراجعة أمنية · صلاحيات · RLS · معالجة الفشل | ميزات · شاشات · محتوى · تجربة الاستخدام |
| **الهجرات** | تعديل الحرّاس والصلاحيات على ما هو قائم | إنشاء الجداول والدوال الجديدة |
| **القاعدة** | لا يضيف ميزات | لا يعيد كتابة حارس أمني دون قراءة سببه أولاً |

**قاعدة الحسم عند التداخل:** إن لمس عملك حارساً أمنياً أو سياسة RLS، اكتب في «رسائل» أدناه
قبل التعديل. وإن لمس عملي شاشة أو تدفّق استخدام، أكتب أنا فيها قبل التعديل.

---

## ٢. قواعد قصيرة

1. **قبل البدء:** اقرأ الجدول أدناه، ثم تحقّق أن فرعك ليس متأخراً:
   ```bash
   git fetch origin main && git rev-list --count HEAD..origin/main
   # 0 = محدَّث · رقم كبير = توقّف وأعد بناء فرعك على main أولاً
   ```
2. **عند البدء:** أضف صفّاً لنفسك في «العمل الجاري» **قبل** أن تعمل، لا بعده.
3. **عند الانتهاء:** انقل صفّك إلى «السجل»، واذكر ما أُنجز وما لم يُنجز.
4. **لا تعمل على منطقة يحملها غيرك.** إن اضطررت، اكتب ذلك في «رسائل».
5. **`main` لا يُدفع إليه مباشرة.** كل عمل عبر فرع و PR.

---

## ٣. العمل الجاري

| المنطقة | مَن | الفرع | منذ | الحالة |
|---|---|---|---|---|
| **المرحلة ٢ — التفعيل**: الرحلةُ الذهبيّة تُثبَت | المصحّح | `claude/project-review-analysis-dt7lsy` | 2026-09-04 | **جارٍ الآن.** بوّابةٌ تُثبت أنّ صاحبَ الحقّ يمرّ، لا ما يُمنع |
| **المرحلة ١ — التثبيت**: CI · صلاحيات الدوال · تدقيق عملية الاستثمار | المصحّح | `claude/project-review-analysis-dt7lsy` | 2026-09-04 | **انتهت ودُمجت** (#48). والهجرةُ مطبَّقةٌ على الإنتاج |
| العقود · مقدّمو الخدمة · المساعد · معالجة الأخطاء | المصحّح | `claude/project-review-analysis-dt7lsy` | 2026-08-19 | **انتهى.** والمحجوز على الإنتاج طُبِّق كلُّه (الهجرات · سحب الدوال الخمس) |
| ممرّ الصادر · طلب الاهتمام · الردّ الآلي على الشكاوى | المصحّح | `claude/project-review-analysis-dt7lsy` | 2026-09-03 | **انتهى ودُمج** (#45 · #46 · #47). موقوفٌ على المالك: `CRON_SECRET` · زرّ المتّجهات |
| الواجهة والمنتج (زر الاعتماد · `paused_by_owner` · الجوّال) | **شاغر — للبنّاء** | — | — | مفصّلة في [`handoff-2026-08-20.md`](handoff-2026-08-20.md) §٣ |

---

## ٤. رسائل

القناة بين الطرفين. الأحدث أولاً. اكتب هنا حين تبدأ شيئاً يمسّ منطقة غيرك، أو حين تريد
مراجعة شيء بعينه، أو حين ترى في تغيير غيرك خطأً — **بدل التراجع الصامت**.

### 2026-09-04 (الأحدث) · من المصحّح → إلى البنّاء · 🔴 **الهجراتُ وحدَها لا تُنتج قاعدةً محميّة**

**هذا أخطرُ ما وجدتُه اليوم، وهو بنيويٌّ لا خطأً في سطر.**

الهجرة `20260817120000_document_existing_policies_and_guards.sql` تُنشئ نحوَ
أربعين سياسة، **وليس فيها `enable row level security` ولا مرّة واحدة**. ولم
تحتج إليها، لأنّ الأساسَ كان قد فعّلها — خارج مجلّد الهجرات حيث لا شيء يُتابَع.

فلو أُعيد بناءُ القاعدة من مجلّد الهجرات وحده: السياساتُ كلُّها تُنشأ، وتظهر في
`pg_policy`، **ولا تحرس شيئاً** — ولا رسالةَ تحذيرٍ واحدة. الإنتاجُ سليم (تحقّقتُ:
مفعّلةٌ على ٥٠ جدولاً من ٥٠)، لكنّ **الهجراتِ ليست مكتفيةً بذاتها**.

فوضعتُ التفعيلَ في `scripts/base-schema.sql` مع بيانِ السبب. وإن نقلتَ الأساسَ
إلى الهجرات يوماً — وهو الصواب — فابدأ بهذا السطر.

**وشيءٌ ثانٍ من العائلة نفسِها:** `is_admin()` في الإنتاج `security definer`،
وكانت تجهيزتي تُعلنها `stable` فقط. وحمايةُ الصفوف مفعّلةٌ على `profiles`، فدالّةٌ
تعمل بصلاحية المنادي لا تقرأ صفَّه وتُرجع «ليس مديراً» **للمدير نفسِه** — وهي
تُنادى من داخل عشرات السياسات، فيصير الرفضُ عامّاً وصامتاً. أسقطت ١٠ فحوصٍ حين
فعّلتُ الحماية، ولولا ذلك لبقيت مستورة.

---

### 2026-09-04 · من المصحّح → إلى البنّاء · 🔴 **دورُ `farmer` غيرُ موجود**

**ابنِ على هذا، فالأرجح أنّك تظنّ ما كنتُ أظنّ.**

عمودُ `profiles.role` في الإنتاج نوعٌ تعداديٌّ اسمه `user_role`، وقيمُه ثلاث:

```
('investor', 'admin', 'field_agent')
```

**لا `farmer` فيها.** والافتراضيُّ `investor`. فالمنصّةُ لا تفرّق بين مزارعٍ
ومستثمرٍ بالدور أصلاً — تفرّق بالمِلكيّة: `owner_id = auth.uid()`.

واكتشفتُه لأنّ بوّاباتي الأربع كلَّها كانت تكتب `role text default 'farmer'`،
وعمودٌ نصّيٌّ يقبله والعمودُ الحقيقيُّ يرفضه. لم يتغيّر حكمُ فحصٍ واحد — الحرّاسُ
يفحصون `= 'admin'` أو المِلكيّة، ولا يعنيهم اسمُ غير المدير — **ولذلك نجا
الخطأُ في ١٤٧ فحصاً دون أن يلحظه أحد**.

وهذا هو فرقُ التجهيزة عن البديل المختصر: العطبُ ليس أنّ الفحص يفشل، بل أنّه
**ينجح على قاعدةٍ لا يمكن أن توجد**.

فأُضيف `scripts/base-schema.sql`: جداولُ الأساس وأنواعُه **مقروءةً من الإنتاج**
لا من الذاكرة، تشترك فيها البوّابات الأربع. و`'farmer'` الباقيةُ في
`src/app/api/leads/route.ts` صحيحةٌ ولا تُمسّ — تلك حقلُ «صفة المُراسِل» في
`leads`، لا دورَ حساب.

---

### 2026-09-04 · من المصحّح → إلى البنّاء · **كلُّ بوّاباتنا كانت تسأل سؤالاً واحداً**

انتبهتُ إلى شيءٍ وأنا أبدأ المرحلة ٢، وأحسبه يعنيك أكثر ممّا يعنيني.

في بوّاباتنا الثلاث ١٢٤ فحصاً، **٤٦ منها تُثبت أنّ شيئاً يُرفض**. ولا واحدٌ منها
يُثبت أنّ مزارعاً شرعيّاً يستطيع أن يُنهي شيئاً.

ومعنى ذلك حرفيّاً: **منصّةٌ ترفض كلَّ كتابةٍ من كلّ أحد تنجح في الفحوص الـ١٢٤
كلِّها.** كلُّ حارسٍ أُثبت أنّه يوقف الغريب، ولا حارسَ أُثبت أنّه يُدخل صاحبَ
الحقّ. وهذا ليس احتمالاً نظريّاً — إنّه الناتجُ العاديّ لتضييق سياسةٍ بندٍ واحدٍ
أكثر ممّا يجب، وهو بالضبط ما كنّا نفعله أسابيع.

فأضفتُ `verify-farmer-journey.sh`: لا فحصَ رفضٍ واحدٍ فيها. تمشي بمزارعٍ من
قاعدةٍ فارغة إلى عرضٍ منشورٍ وطلبِ اهتمامٍ في بريد الموظّف، وتؤكّد في كلّ خطوة
أنّ **الفعلَ نُفِّذ ومسّ صفّاً**.

**وجرّبتُها بتضييقٍ واحد:** نزعتُ كلمة `'rejected'` من `USING` في
`export_offers_owner_update` — كلمةٌ واحدة. فسقط **عشرةُ فحوص**، والرحلةُ
التجاريّةُ كلُّها انقطعت: المزارعُ لا يستطيع إصلاح ما رُدّ عليه، فلا يُنشر عرض،
فلا يصل مشترٍ. **ولم يسقط أيُّ فحصٍ من الـ١٢٤.**

> **فإن ضيّقتَ سياسةً يوماً** — وهو عملٌ مشروع — شغّل هذه البوّابة قبل الدفع.
> الفشلُ الصامت فيها يظهر هكذا: «نُفِّذ ولم يمسّ صفّاً»، وهو نفسُه ما تراه
> الشاشةُ نجاحاً.

---

### 2026-09-04 · من المصحّح → إلى البنّاء · 🔴 **أعتذر: عمل طلبين لم يُسجَّل هنا**

**هذا أوّلاً، لأنّه خطئي.** الطلبان **#46** و**#47** بُنيا ودُمجا ولم أكتب عنهما سطراً هنا. وهذا
بالضبط ما يُنتج الحادثةَ التي وُجد الملفُّ لمنعها. إن كنتَ قد عملتَ في هذه المناطق بين
3 و4 سبتمبر فراجعْ `main` قبل أيّ شيء — الأرجح أنّ ما تراه ناقصاً قد أُنجز.

**#46 — طلبُ الاهتمام · حفظُ جواب المساعد · سحبُ `anon`**
- جدول `export_offer_interests`: المشتري يرسل اهتماماً **بلا حساب**، وسياسةُ الإدخال تسمح
  فقط مقابل عرضٍ منشور وبحالةٍ ابتدائيّة ثابتة. القراءةُ للمدير وحده، **ولا سياسةَ حذفٍ لأحد**.
- `feedback`/المساعد: أعمدة `answer_text` و`promoted_*`، ودالّة `promote_assistant_answer`.
  و`log_assistant_question` صارت خماسيّةَ الوسائط (سُقطت الرباعيّة).
- **سُحب `anon`** عن `log_assistant_question` و`run_system_check`. و`/api/health` صار يستعمل
  عميلَ الخدمة ولا يُرجع تفاصيل ما لم يُضبط `CRON_SECRET`.

**#47 — الردُّ الآليّ على الشكاوى بعد ربع ساعة**
- `support_policy` صفٌّ واحد (المهلة · التشغيل · الأنواع) — **قاعدةُ عملٍ في صفٍّ لا في كود**،
  وصفحةُ الملاحظات تقرأ وعدَها منه.
- أعمدة `ai_reply`/`ai_replied_at`/`ai_reply_engine` في `feedback`، **منفصلةٌ عن `admin_reply`**
  عمداً: كي يُعرف مَن كتب، ولا يمحو ردُّ المدير ما قرأه الزائر.
- دالّتان مسحوبتان عن `anon` و`authenticated`، للخادم الموثوق وحده:
  `feedback_awaiting_auto_reply` و`record_feedback_auto_reply`.

> **إن لمستَ `feedback`:** لا تكتب الردَّ الآليَّ في `admin_reply`، ولا تحذف إعادةَ فحص
> `admin_reply is null` من داخل `UPDATE` في `record_feedback_auto_reply`. تلك الإعادةُ هي
> الميزةُ كلُّها: نداءُ النموذج يستغرق ثوانيَ وقد يردّ الموظّفُ داخلها، فالإنسانُ يسبق.

---

### 2026-09-04 · من المصحّح → إلى البنّاء · **المرحلة ١ (التثبيت) — وما لا تفتحه بعدُ**

المالكُ عرض دراسةً خارجيّة على الجرد، ورتّبت سبعَ مراحل. **بدأتُ المرحلة ١ وحدها**، وهي
تثبيتٌ لا ميزات — والدراسةُ نفسُها تنهى عن فتح ما بعدها قبل إغلاقها.

**ما ألمسه الآن** (فقُل لي إن كان يصطدم بك):
1. `.github/workflows/web.yml` — يعمل على `push` و`pull_request` معاً، فيُشغَّل مرّتين.
2. بوّابتا SQL (`verify-export-offers.sh` · `verify-support-auto-reply.sh`) إلى CI. وهما
   تبنيان عنقوداً مؤقّتاً، فسأجعلهما تعملان بمستخدمٍ عاديّ لا بـ`root` وحده.
3. `confirm_investment` — تدقيقٌ كامل وناتجٌ صريح. تفصيلُه في الرسالة التالية.

**وما لا أفتحه، وهو لك إن أردته لاحقاً:** Trust Score · Digital Farm Passport ·
Marketplace Engine · تحويلُ المساعد إلى Copilot · Funnel Analytics · Notification Center ·
Admin Command Center · البحثُ الهجين · Document Intelligence. **لا تبدأها قبل أن تُغلق
المرحلة ١**، فهذا نصُّ الدراسة لا رأيي.

---

### 2026-09-04 · من المصحّح → إلى المالك والبنّاء · 🔴 **الملفُّ يُحذف بعد أن يُجمَّد صفُّه**

**وجدتُه في مراجعة الرفع، ولم أُصلحه لأنّه قرارُ تصميمٍ لا خطأٌ مطبعيّ.**

صفوفُ `export_offer_evidence` تُجمَّد كما ينبغي: سياسةُ الكتابة تشترط
`o.status in ('draft','rejected')`، فما إن يُقدَّم العرضُ حتى لا يُعدَّل دليلُه
ولا يُحذف.

**لكنّ الملفَّ نفسَه في `storage.objects` لا تحكمه تلك الشرط.** سياسةُ الحذف:

```sql
evidence_delete_own — DELETE — authenticated
  using (bucket_id = 'evidence'
     and (storage.foldername(name))[1] = auth.uid()::text)
```

لا ذكرَ للحالة. فالمزارعُ الذي نُشر عرضُه يستطيع حذفَ الملفّ من المخزن، ويبقى
الصفُّ المجمَّد يشير إلى ملفٍّ غير موجود. وهذا يمسّ أطروحةَ المنصّة نفسَها: نحن
لا نبيع وصولاً إلى سوق، بل نبيع **إثباتاً** — وإثباتٌ يمكن لصاحبه سحبُه بعد
الاعتماد ليس إثباتاً.

والأمرُ نفسُه ينطبق على `stage_evidence` و`milestone_evidence` و`custody_evidence`
— وكلُّها تحمل عمود `storage_path`، فالمقترحُ أدناه ينطبق عليها بتغيير الجدول.

**وحدُّ الضرر:** `export_offer_evidence` فيه عمود `sha256`. فالبصمةُ محفوظة، أي
أنّ **الاستبدالَ** مكشوف — لا يستطيع أحدٌ وضعَ ملفٍّ آخر مكانَه ويمرّ. لكنّ
البصمةَ تُثبت أنّ ملفّاً بهذا المحتوى كان موجوداً، ولا تُغني عنه: المشتري الذي
يفتح الدليلَ بعد النشر يجد لا شيء. فالخطرُ **فقدُ الأثر لا تزويرُه**.

**ولم أُصلحه من طرفي لسببين:**

1. **مقايضةٌ حقيقيّة**: مَن رفع ملفّاً خطأً في مسوّدة يجب أن يستطيع حذفَه. فالمنعُ
   المطلق ليس صواباً، والصوابُ شرطٌ من نوع «لا يُحذف ما يشير إليه صفٌّ خرج من
   المسوّدة».
2. **سياساتُ `storage.objects` ليست في الهجرات أصلاً** — تُدار من لوحة Supabase.
   فنقلُها إلى هجرةٍ قرارٌ في مكان إدارة هذا الحدّ، لا مجرّد تعديل.

**المقترح** (لم يُطبَّق — ينتظر قرار المالك):

```sql
drop policy if exists evidence_delete_own on storage.objects;
create policy evidence_delete_own on storage.objects for delete to authenticated
using (
  bucket_id = 'evidence'
  and (storage.foldername(name))[1] = auth.uid()::text
  -- ولا يُحذف ما اعتمد عليه شيءٌ خرج من المسوّدة
  and not exists (
    select 1 from export_offer_evidence e
      join export_offers o on o.id = e.offer_id
     where e.storage_path = storage.objects.name
       and o.status not in ('draft','rejected'))
);
```

---

### 2026-09-04 · من المصحّح → إلى البنّاء · **`prevent_self_role_escalation` تُرجِع بصمت**

ملاحظةٌ لا أطلب تغييرها، لكن يجب أن تُعرف:

```sql
if new.role is distinct from old.role and auth.uid() is not null and not is_admin() then
  new.role := old.role;   -- يُعاد الدورُ القديم، والتحديثُ «ينجح»
end if;
```

فمن حاول ترقيةَ نفسِه يُقال له «حُفظ» ولم يُحفظ. وهذا **مقصودٌ ومعقول** أمام
مهاجم: لا تُخبره أنّ هناك حارساً. لكنّه من عائلة «النجاح الصامت» التي عالجناها
في `confirm_investment` — فإن غيّرتَ هذا السلوك يوماً، غيّره برفع خطأٍ صريح لا
بصمتٍ آخر.

---

### 2026-09-04 · من المصحّح → إلى البنّاء · **`confirm_investment`: لماذا أعدّلها**

**هذه عمليّةُ مال، لا دالّةٌ عاديّة.** وضعُها اليوم:

```sql
if not is_admin() then raise exception 'not authorized'; end if;   -- ✅ سليم
select ... where id = p_investment_id and status = 'pending';
if v_project_id is null then return; end if;                        -- ⚠️ صمتٌ
select ... for update;                                              -- ✅ يقفل الصفّ
```

القفلُ والتفويضُ سليمان ولا أمسّهما. **المشكلةُ في السطر الصامت**: إن كان الاستثمار محذوفاً،
أو مؤكَّداً سلفاً، أو مرفوضاً — تخرج الدالّةُ بلا خطأ، فيقرأ المستدعي «نجاح». وهذا نفسُ الفخّ
الذي وقعتُ فيه ثلاث مرّات مع RLS: **غيابُ الخطأ ليس دليلَ حدوث**.

ما أضيفه:
- `investment_events` — سجلٌّ **لا يُعدَّل ولا يُحذَف** (زنادٌ يرفض `UPDATE` و`DELETE`)،
  يقيّد كلَّ محاولة: الفاعل، الوقت، النتيجة، السبب، و`shares_sold` قبلها وبعدها.
- الدالّةُ تُرجع نتيجةً مصنّفة بدل `void`، **وتقيّد الرفضَ كما تقيّد النجاح** — فمحاولةٌ
  مرفوضةٌ لتجاوز المتاح حدثٌ يستحقّ الحفظ، لا صمتاً.
- **سحبُ التنفيذ عن `anon` فقط.** لا ألمس `authenticated`: المديرُ نفسُه دورُه
  `authenticated`، والحارسُ `is_admin()` داخل الجسم.

> ⚠️ **ولا أسحب `is_admin()` ولا `can_see_contract()` عن أحد** رغم أنّهما مفتوحتان لـ`anon`.
> تحقّقتُ: كلتاهما تُنادى **داخل سياسات RLS**، والدورُ المنفِّذ للاستعلام يحتاج `EXECUTE`
> عليهما. سحبُهما يكسر كلَّ قراءةٍ في المنصّة. إن رأيتَهما في تدقيقٍ أمنيّ فلا تسحبهما.

---

### 2026-09-03 · من المصحّح → إلى البنّاء · **زرٌّ في `/admin` يحسب متّجهات المعرفة**

**دخلتُ `src/app/admin/page.tsx` وأضفتُ قسماً.** أسجّله لأنّ اللوحة ليست في جدول المناطق،
ولأنّ `src/lib/embedding.ts` منطقتُك — وإن لم ألمسه.

**السبب ليس راحة.** أضفتُ ١٣ مُدخل معرفة عن الصادر، وطريقُ إعطائها متّجهات كان أمراً طرفياً
يحمل سرَّين. وصاحبُ المنصّة **يعمل من جوّال ولا حاسوب عنده**. فالتعليمةُ لم تكن التفافاً عنده
بل جداراً، والميزةُ التي تحرسها — نصفُ الاسترجاع الدلالي على المُدخلات الجديدة — بقيت مطفأةً
ببساطة. والمفتاحان **على الخادم أصلاً**: المساعد يضمّن كل سؤالٍ بالمزوّد نفسِه، والتسجيلُ
بالهاتف يستعمل مفتاح الخدمة. فلا ثقةَ جديدة هنا، إنّما مشغّلٌ لم يعد مضطرّاً لحمل أيٍّ منهما.

**ولم أنسخ المنطق.** أخرجتُ محرّك الحساب إلى `src/lib/backfillEmbeddings.ts`، ويناديه
الاثنان: الأمرُ الطرفي والزرّ. فلا يملك أحدُهما رأياً خاصاً في «أيُّ صفٍّ متقادم» — وهي
القاعدةُ الوحيدة التي لا يجوز أن تنقسم. والفرقُ بينهما حجمُ العمل فقط: الأمرُ يُنهيها،
والزرُّ **دفعةٌ واحدة لكلّ ضغطة**، لأنّ الطلبَ الخادميّ يُقتل على ساعته بلا خطأٍ يقرؤه أحد.

**و`remaining` يُعاد حسابُه من الصفوف لا يُعدّ تنازلياً** — وإلّا لأنقصته دفعةٌ فاشلة وأعلنت
الشاشةُ عملاً لم يحدث. هذا وغيرُه مفحوصٌ في `scripts/verify-embedding-backfill.ts` بقاعدةٍ
مزيّفة ومزوّدٍ مزيّف: ٢٤ فحصاً، منها أنّ دفعةً ساقطة **لا تُلغي** ما بعدها، وأنّ الصفَّ الذي
ترفضه القاعدة يبقى في الباقي ويُسمّى بعنوانه.

**وملاحظةٌ لك:** رأسُ `src/lib/supabase/admin.ts` يقول إنّ «المنادي الوحيد هو فعلُ التسجيل».
صار له منادٍ ثانٍ، فعدّلتُ الجملة. وهي كتابتُك.

### 2026-09-03 · من المصحّح → إلى البنّاء · **ع-4 أُغلق: الدوال الخمس في المستودع الآن**

سحبتُ الخمسَ الباقيةَ من الإنتاج بـ`pg_get_functiondef` — لا كتابةً من مُنادِيها —
وأودعتُها `20260903120000_document_dashboard_functions.sql`:
`farmer_season_records` · `public_farmer_profile` · `run_system_check` ·
`log_assistant_question` · `match_knowledge_entries`.

**وأثبتُّ أنها نسخةٌ طبقُ الأصل لا قراءةٌ تقريبية:** طبّقتُ الملفّ كلَّه على الإنتاج داخل
معاملةٍ تنتهي بـ`rollback`، فمرّ بلا خطأ، وخرجت بصماتُ `md5(pg_get_functiondef(...))`
الخمسُ **مطابقةً** لما كان قبله، وكذلك `proacl` الخمس. أي أن تطبيقه على الإنتاج لا يغيّر
شيئاً، وتطبيقه على قاعدةٍ جديدة يُنتج هذه الدوالَّ بهذه الأبواب بعينها.

**والأبوابُ هي الجزء الذي لا يجوز إغفاله.** `create or replace` على دالّةٍ قائمةٍ لا يمسّ
صلاحيتها — لكنّ إنشاءها لأوّل مرّة يمنح `PUBLIC` حقَّ التنفيذ افتراضاً. فلو وثّقتُ الدوالَّ
وسكتُّ عن الصلاحيات، لكان ملفّي هذا **يُلغي هجرة `20260903090000`** ويعيد لِـ`anon` نقطةَ
المال التي سُحبت منه قبل يومين. لذلك في ذيل الملفّ كتلةُ `revoke`/`grant` تُعيد كتابة
`proacl` الخمس كما قرأتُها من الإنتاج حرفياً.

**وخرجت من الاستخراج ملاحظةٌ لم تكن مكتوبةً عند أحد — `run_system_check`:**

جدول `system_checks` عليه RLS وسياسةٌ واحدة: `system_checks_admin_read`. أي أن أحدهم قرّر
أن هذه الأرقام للإدارة. لكنّ الدالّة `SECURITY DEFINER` و`anon` يملك تنفيذَها، فأيُّ زائرٍ
يحمل المفتاح العام — وهو كلُّ زائر — ينادي النقطة فيأخذ ما وُضعت السياسةُ لحجبه: **كم
حساب إدارةٍ في المنصّة** (و`admins: 0` جوابٌ يستحقّ المعرفة لمن ينوي سوءاً)، وكم طلباً
ينتظر مراجعة، وكم فرصةً نُشرت دون اعتماد. وليست قراءةً فقط: كلُّ نداءٍ **يُدخل صفّاً**
ويُجري حذفاً على الجدول.

**لم أغلقها في هذا الملفّ عمداً** — سحبُ صلاحيةٍ تغييرُ سلوك، ويلزمه أوّلاً فحصُ مَن
ينادي الدالّة، بالترتيب الذي علّمَنا إياه #42: الكودُ ينتقل إلى عميل الإدارة، **ثم**
تُسحب الصلاحية. والعكسُ يكسر الصفحة. الأرجح أن يكون شكلُها كشكل `farmer_season_records`
تماماً. **هي لك أم لي؟** أراها تصحيحاً وأستطيع أخذها، فقل إن كنتَ بدأتَها.

**وواحدةٌ منها مفتوحةٌ لِـ`anon` عن قصد ولا تُلمس:** `log_assistant_question`. أخبرني
المالك أنّ سؤالَ الزائر حين لا يجد المساعدُ جواباً **يُخزَّن ليُبحث عنه** — تلك ميزةٌ لا
ثغرة. والطريق `src/app/api/assistant/route.ts` ينادِيها بعميل الجلسة، فسحبُ `anon` منها
اليوم يكسرها. كتبتُ ذلك في رأسها في الملفّ كي لا يقرأها مدقّقٌ بعدنا فيظنَّها سهواً
ويغلقها.

### 2026-09-03 · من المصحّح → إلى البنّاء · **دخلتُ لَينك بطلبٍ من المالك: صفحة `/export`**

المحتوى العام لَينك لا لَيني، وأسجّل هذا هنا لا لأعتذر بل لتعرفه قبل أن تبدأ شيئاً عليه.
طلب المالك نشرَ دراسة الصادر على الموقع، فبنيتُ `/export` وربطتُها في «مشاريع ودراسات»
وعرّفتُها في `pageHelp`.

**واتّبعتُ عرفك في `/guide`:** لا رقمَ في نثر. كلُّ الأرقام في `src/lib/exportTrade.ts`
ومعها مصادرُها، والصفحة تعرض المصدرَ بجانب الرقم. فتحديثُ تعريفةٍ أو موعدِ لائحةٍ سطرٌ
واحد، ولا تختلف فقرتان على رقم.

**وفرّقتُها عن `/arc-canal` عمداً:** تلك حسابُنا ونوقّعه، وهذه **لا رقمَ فيها من قياسنا** —
كلُّه مقروءٌ من غيرنا ومنسوبٌ إليه. والخلطُ بينهما يُفقد الاثنتين الثقة، فكُتب ذلك في رأس
الصفحة صراحةً.

**وتركتُ خارجها** الجزءَ الهندسي كلَّه: ما نبنيه وكيف. قارئُ الصفحة مزارعٌ أو مصدِّرٌ أو
مشترٍ، وحاجتُه شكلُ البوّابات التي عليه اجتيازها لا خطّتُنا.

**وملاحظةٌ صغيرة في ملفّك:** تعريف `/arc-canal` في `pageHelp.ts` يقول إنها «مراجعة… تعرض
كل ادّعاء مع حكمٍ عليه»، ورأسُ الصفحة نفسِها يقول إن تلك المراجعة أُزيلت وإن الصفحة صارت
دراسةً قائمةً بذاتها. التعريفُ يصف صفحةً لم تعد موجودة. لم ألمسه — هو نصُّك.

### 2026-09-03 (لاحقاً) · من المصحّح → إلى البنّاء · **مواصفةُ مُنتِج عرض التصدير**

بنيتُ في `zadim` **النصف المستقبِل** من جسر التصدير (PR #7 هناك): دفترُ واردٍ ودالّةُ
إدماجٍ تحوّل «عرضاً موثّقاً» إلى إرسالية، بأربعةَ عشرَ حارساً مُثبَتاً. العقد كامل في
`zadim/docs/14-bridge-contract.md`.

**ثم ذهبتُ أبني المُنتِج هنا فوقفت.** لأن ما يطلبه العرض **غيرُ مسجَّلٍ في سودجري**،
وبناءُ مُنتِجٍ يخترعه يخالف قاعدتنا الثانية: «لا بياناتٍ وهمية في مسارٍ حقيقي». فهذه
المواصفة بدل كودٍ يكذب.

**فحصتُ ولم أفترض. ما يحتاجه العرض، وأين هو:**

| ما يطلبه العرض | الحال في سودجري |
|---|---|
| إذنُ صاحب السجلّ | ✅ `profiles.publish_record` — جاهز |
| إحداثيّاتُ الدليل ووقتُ التقاطه | ✅ `stage_evidence.latitude/longitude/captured_at` — موجودةٌ ويكتبها `addStageEvidence` |
| **الكمّيةُ المنتَجة ودرجتُها** | ❌ **غيرُ موجودة إطلاقاً.** `seasons` فيه `feddans` و`budget_per_feddan` — مُدخلاتٌ لا مُخرَجات. والدفترُ يسجّل `amount` مالاً فقط، لا وزناً |
| **قطعةُ الأرض وإحداثيّاتُها** | ❌ **`seasons` بلا `land_id`.** فيه `location` نصّاً و`station_key`. و`lands` فيه `latitude/longitude/feddans` — فالبيانات موجودةٌ والرابطُ مفقود. (`herds` عنده `land_id`، والمواسمُ لا) |
| **بصمةُ الدليل (sha256)** | ❌ غيرُ مسجّلة. الملفّ في المخزن وبصمتُه لم تُحسب قطّ |

**والثلاثةُ الناقصة كلُّها في لَينك** — إنشاءُ أعمدةٍ وجداول. فأتركها لك ولا ألمسها.

**ولماذا كلٌّ منها ليس تجميلاً:**

1. **الكمّية والدرجة** — بلا وزنٍ ودرجةٍ لا يوجد عرضُ بيعٍ أصلاً. وأقترح جدولاً منفصلاً
   (`season_harvest`) لا عمودين في `seasons`: الموسمُ الواحد يُنتج درجتين بسعرين، وعمودٌ
   واحد يجبرك على اختيار إحداهما. والوحدة **عمودٌ صريح** لا مُفترَضة.
2. **`seasons.land_id`** — لائحةُ منع إزالة الغابات الأوروبية تسري 2026-12-30 وتطلب
   إحداثيّةَ قطعةِ الإنتاج: نقطةً لما دون ٤ هكتارات ومضلَّعاً لما فوقها. و`location` نصّاً
   لا يُقدَّم إلى جمرك. الرابطُ وحده يجعل ما تملكه بالفعل صالحاً للتصدير.
3. **بصمةُ الدليل** — تُحسب من **الملفّ نفسِه** لا من مساره. بصمةٌ من المسار تتطابق
   دائماً ولا تكشف تبديلاً أبداً. وهي ما يجعل «هذه هي الصورة نفسُها» جملةً قابلةً للفحص
   بعد سنتين، حين يطلب المشتري الأصل.

**وحين تجهز الثلاثة أبني المُنتِج**، وشرطاه مكتوبان في العقد: ألّا يُصدر عرضاً لمن لم
يأذن (فحصٌ هنا **إضافةً** إلى فحصه هناك، لا بديلاً — فحمولةٌ تصل من خطأٍ برمجيّ يجب أن
تُردّ عند البابين)، وأن يحسب البصمة من الملفّ.

**وأصلحتُ في الطريق نوعاً كان يكذب:** `StageEvidence` كان يُعلن `url` — عمودٌ لا وجودَ
له — ويُسقط `storage_path` و`captured_at` و`latitude` و`longitude` وهي موجودةٌ وتُكتب.
وصفحةُ الموسم تقرأ `select("*")` ثم تُسند إلى هذا النوع، فغسّل الإسنادُ الخطأ في
الاتجاهين: `evidence.url` يجتاز الفحص وهو `undefined` وقتَ التشغيل، والإحداثيّاتُ التي
تجعل الصورةَ دليلاً محجوبةٌ عمّن يقرأ النوع. **كامنٌ لا حيّ** — لا أحد يقرأ `.url` اليوم.

### 2026-09-03 · من المصحّح → إلى البنّاء · 🔴 **الهجرة التي وعدتَ بها في #42 لم تصل**

راجعتُ #42 و#43 بعد دمجهما. **العملان صحيحان في جوهرهما** — بل إن #42 وجد ثغرةً كنتُ
رصدتُ عائلتَها ولم أصل إليها، وشخّصها بدقّة. و`public_farmer_profile` في #43 صارت في
المستودع تُعيد أربعة أعمدة لا مال فيها، محروسةً بالإذن وبوجود موسمٍ فعليّ. عملٌ جيّد.

**لكن سطراً في رسالة #42 نفسِها لم يُنفَّذ:**

> «السحب من `anon` يأتي في هجرةٍ تالية. نشرُ الكود قبلها لا يكسر شيئاً…»

الترتيب الذي اخترتَه صحيح — الكودُ أولاً هو الاتجاه الآمن. **لكن الهجرة التالية لم تأتِ.**
بحثتُ في كل الهجرات، ومتعدّدَ الأسطر لا سطرياً: لا `revoke` لـ`farmer_season_records`
في أيٍّ منها، وآخرُ هجرةٍ في المستودع هي `20260825120000`.

**فالنصفُ التطبيقيُّ حيٌّ منذ 24 أغسطس، والنصفُ الحدوديُّ لا.** الصفحة كفّت عن طباعة
الأرقام، والنقطةُ ما زالت تخدمها — وهي الثغرةُ التي وصفتَها أنت بنفسك.

**وليست نظرية:** `projects/[slug]/page.tsx:98` يضع
`href={`/farmers/${submitted_by}`}` في كل صفحة مشروعٍ عامّة. فالمعرّف يُقرأ من الصفحة،
والنداءُ بمفتاح `anon`، والناتجُ ميزانيةُ كل موسمٍ وتكاليفُه وإيرادُه.

**والأهمّ — والذي أرجو أن تنتبه له:** #43 تُقرأ كأنها تحمي هذا، ولا تحميه.
`publish_record` مفحوصٌ داخل `public_farmer_profile` وحدها. و`farmer_season_records` لا
تعرفه، ومن ينادي النقطةَ مباشرةً لا يمرّ بدالّة الملف أصلاً. **فمزارعٌ لم يُسأل ولم يأذن
أرقامُه مقروءةٌ اليوم.** الإذنُ يحكم الصفحة، لا المال.

**فكتبتُ الهجرة:** `20260903090000_revoke_farmer_season_records.sql`. وهي حلقةٌ على
`pg_proc` لا توقيعٌ مسمّى، لأن الدالة ما زالت خارج المستودع فتوقيعُها غيرُ معلوم — وتوقيعٌ
خاطئ يُسقط الهجرة على الإنتاج، وهو آخرُ مكانٍ يجوز أن تسقط فيه. وغيابُ الدالة يُخرج
ملاحظةً ولا يُسقط، حتى لا ينكسر `db reset` على قاعدةٍ لم تُنشئها هذه الهجرات أصلاً.

**مُثبَتة على Postgres 16:** `anon` من `t` إلى `f`، و`authenticated` كذلك، و`service_role`
باقٍ — وعلى قاعدةٍ بلا الدالة لا تسقط.

**وما زال عليك سحبُها إلى المستودع** (`supabase db pull`) — فما يُعيده هذا الجسمُ يُستنتج
اليوم من مناديه لا يُقرأ من تعريفه.

### 2026-08-20 · من المصحّح → إلى البنّاء · **عدّلتُ في `admin/canal-images/actions.ts`**

قرأتُ #32 بعد دمجه. **الدلو والسياسات والقيود عمل نظيف** — `credit` إلزامي بقيد في
القاعدة لا بتحقق في النموذج وحده، و`published` تبدأ `false`، و`safeSourceUrl` مع فحوصه في
`verify-media.ts` يغلق `javascript:` على أول `href` يكتبه مدير في صفحة عامّة. ولا أنقض شيئاً
من ذلك.

**واتفق معك في قرارك بعدم وضع `requireAdmin` هنا،** وتعليقك في رأس الملف صحيح: الحدّ في
القاعدة، وفحصٌ في التطبيق يوهم أنه هو الحارس. تركتُه كما هو.

**لكنه لا يغطي شكل الرفض.** `UPDATE` أو `DELETE` تحجبه RLS **لا يُرجع خطأ** — يُرجع صفر
صفوف. والدالتان كانتا تقرآن `error` وحده ثم:

```ts
redirect(`/admin/canal-images?message=${encodeURIComponent("حُفظ.")}`);
```

فمن يرسل طلباً مباشراً إلى الدالة (وهي منشورة كأي Server Function، لا تحرسها الصفحة) يُقال
له «حُفظ.» ولم يُحفظ شيء — **وسيُقال المثل لمديرٍ حقيقي يوم تتوقف سياسة عن المطابقة**.
فأضفتُ `.select("id")` وفحص الطول. **وهذا فحص نتيجة لا فحص صلاحية** — الفرق جوهري، والحدّ
باقٍ حيث وضعتَه.

وفي الحذف أوقفتُ التنفيذ عند صفر صفوف **قبل** لمس الدلو: ترتيبك (الصفّ ثم الملف) مبنيّ على
أن الصفّ ذهب فعلاً، ولو مضى إلى حذف الملف والصفّ باقٍ لأنتج بالضبط الصورةَ المكسورة على
صفحة عامّة التي رتّبتَهما لتفاديها.

**ونقطة تخصّ تعليقك على `recordCanalImage`:** تقول إن `.select()` لا تُضاف لأن سياسة القراءة
سترفض الصفّ خارجاً بما أن `published` تبدأ `false`. السياسة كما كتبتَها أنت هي
`using (published or is_admin())` — فالمدير يقرأ صفّه غير المنشور. أي أن `.select()` آمنة
هناك أيضاً. لم ألمس الإدراج (هو يفحص `error` أصلاً فلا تأكيد كاذب فيه)، وأتركها لك لأنك قد
تكون رأيتَ سلوكاً عملياً لا أراه من هنا.

وأخيراً: رسائل PostgREST الخام كانت تُمرَّر في `?error=` وفي `UploadResult.message`. تكشف
أسماء الجداول والقيود والسياسات، فصارت تُسجَّل ويُعرض نص ثابت — نفس نمط
`admin/providers/actions.ts`. أبقيتُ مسار الملف ظاهراً في رسالة الملف اليتيم: يتيمٌ بلا اسم
يتيمٌ لا يُنظَّف.

### 2026-08-20 (لاحقاً) · من المصحّح → إلى البنّاء · **تصحيح لرسالتي السابقة**

**كنتُ أخطأتُ، وهذا تصحيح لما قلتُه لك في النقطة ٣ أدناه.**

قلتُ لك إن هجرة `20260819180200` تقترن بمسار المساعد وحده. ليست كذلك:
`check_assistant_rate_limit` لها **أربعة** مستدعين — `api/assistant`, `api/feedback`,
`api/leads`, `api/diagnostics` — وكنتُ قد صحّحتُ واحداً منها فقط. لو نزلت الهجرة على تلك
الحالة، لَتوقّف التحديد صامتاً في المسارات الثلاثة الأخرى: تُسحب الصلاحية، فيفشل النداء،
والسلوك القديم هناك كان «سجّل الخطأ وأكمل» — أي بابٌ مفتوح بلا أي أثر ظاهر.

**ما فعلتُه:** `assistantRateLimit.ts` → **`rateLimit.ts`**، والدالة صارت
`checkRateLimit(scope, ip)` بمفتاح `scope:ip` — فلكل مسار حصّته بدل حصّة واحدة مشتركة.
والمسارات الأربعة كلها تستدعيها الآن.

**واحتفظتُ لكل مسار بنيّته كما كتبتَها أنت، ولم أوحّدها:**

| المسار | عند تعذّر التحقق | لماذا |
|---|---|---|
| `api/assistant` | **يفشل مغلقاً** (503) | نموذج مدفوع — بلا تحديد تصير الفاتورة مفتوحة |
| `api/feedback` · `api/leads` | **يفشل مفتوحاً** | فقدان رسالة عميل أسوأ من رسالة مكرّرة، وهو ما شرحه تعليقك في `leads` |

`tier: "unavailable"` في القيمة المُعادة هو ما يفرّق بين «مُنع لتجاوزه الحدّ» و«تعذّر
التحقق» — لا تُسقطه إن بنيتَ فوق هذا الملف.

**ما زال الترتيب إلزامياً:** الهجرة أولاً، ثم الكود. لكن الاقتران الآن رباعي لا ثنائي.

### 2026-08-20 · من المصحّح → إلى البنّاء

سلّمتُك تقريراً كاملاً في [`handoff-2026-08-20.md`](handoff-2026-08-20.md). ثلاث نقاط
تمسّ عملك القادم مباشرةً:

1. **أي دالة SQL جديدة تكتبها لن تكون قابلة للتنفيذ من المتصفح تلقائياً.** أضفتُ
   `alter default privileges ... revoke execute on functions from public`. إن احتاجت
   دالتك أن تُستدعى من العميل، امنحها الصلاحية صراحةً في هجرتك.
2. **مراحل العقود:** الاعتماد والدفع صارا للعميل أو الإدارة فقط. أخفِ زر «اعتماد» عن
   مقدّم الخدمة بدل أن ينتظر رفض قاعدة البيانات.
3. **`active` لمقدّم الخدمة صار إدارياً فقط.** إن أردتَ «إيقاف مؤقت ذاتي» فأضف عموداً
   منفصلاً ولا توسّع `active`.

وإن رأيتَ في أيٍّ من تغييراتي خطأً، اكتبه هنا. أنا أخطأتُ فعلاً حين بنيتُ أسبوعاً على
نسخة قديمة.

---

## ٥. المناطق ومالكوها

خريطة سريعة تمنع التصادم. «المالك» يعني: من يُسأل قبل التغيير، لا من يملك الحق وحده.

| المنطقة | الملفات |
|---|---|
| المصادقة والهوية | `src/app/login/`, `src/lib/phone*.ts`, `src/app/reset/`, `src/app/auth/` |
| المال والاستثمار | `src/app/projects/`, `src/lib/config.ts` (`INVESTMENT_LIVE`), `confirm_investment` |
| العقود والخدمات | `src/app/contracts/`, `src/app/services/`, `src/lib/services.ts` |
| المواسم والقطعان والأراضي | `src/app/seasons/`, `src/app/herds/`, `src/app/lands/` |
| المساعد الذكي | `src/app/api/assistant/`, `src/lib/{retrieval,embedding,engines,localAnswer,answerCache}.ts` |
| تحديد المعدّل | `src/lib/rateLimit.ts` — **مشترك بين أربعة مسارات**، لا يخصّ المساعد وحده |
| قاعدة البيانات | `supabase/migrations/` — **أي تغيير هنا يمسّ الجميع** |
| تطبيق الجوّال | `mobile/` |
| المراجع والبيانات | `src/lib/{reference,marketData,cropBenchmark,faostat}*`, `docs/` |

---

## ٦. السجل

بالأحدث أولاً.

### 2026-08-20 · الجولة الرابعة — إغلاق ما يخصّني · المصحّح

**الفرع:** `claude/project-review-analysis-dt7lsy` · `npm test` (typecheck + lint + verify)
و`build` نظيفة.

| ما أُصلح | أين |
|---|---|
| **خطأ في عملي أنا:** صحّحتُ مستدعياً واحداً لـ `check_assistant_rate_limit` من أصل أربعة — الهجرة كانت ستُعطّل التحديد صامتاً في `feedback` و`leads` و`diagnostics` | `lib/rateLimit.ts` (كان `assistantRateLimit.ts`) + المسارات الأربعة |
| المفتاح صار `scope:ip` فلكل مسار حصّته، مع الحفاظ على نيّة كل مسار (مغلق للمساعد، مفتوح للبقية) | نفس الملفات |
| **ص-2 جزئياً:** تسجيل الدليل صار يتأكد أن الملف موجود فعلاً في المخزن، لا أن صفّاً كُتب | `lib/evidenceFile.ts` + `contracts` · `seasons` · `lands/documents` |

**عن `evidenceFile.ts` — إرشاديّ عمداً:** سياسات مخزن `evidence` خارج المستودع (ص-1)، فلا
نعرف إن كان الرافع مسموحاً له بـ `LIST` على مجلده أم بالقراءة المباشرة فقط. إن فشل السرد،
يُسجَّل تحذير و**يُقبل الصفّ كما كان**. يشدّ الفحص حيث تسمح السياسة، ولا يخترع طريقة جديدة
لفشل الرفع. حين تدخل السياسات المستودع، يصير الفرع ذاك رفضاً بسطر واحد.

**تصحيح في العدّ:** الدوال الغائبة عن المستودع **خمس لا أربع**. بحثي الأول كان بسطر واحد،
فأفلت `match_knowledge_entries` لأن نداءه ممتدّ على سطرين. القائمة الكاملة:
`farmer_season_records` · `log_assistant_question` · `match_knowledge_entries` ·
`public_farmer_profile` · `run_system_check`.

**وبعد إعادة البناء على `main`** (وصل #32: دلو `media` وشاشة صور القناة) راجعتُ الوارد
الجديد في منطقتي:

| ما أُصلح | أين |
|---|---|
| `updateCanalImage` و`deleteCanalImage` كانتا تقولان «حُفظ.» / «حُذفت» عند رفض RLS الصامت | `admin/canal-images/actions.ts` |
| الحذف يتوقف عند صفر صفوف قبل لمس الدلو — وإلا صورة مكسورة على صفحة عامّة | نفس الملف |
| رسائل PostgREST الخام لم تعد تُمرَّر في `?error=` ولا في `UploadResult` | نفس الملف |

**ولم ألمس** قرار البنّاء بترك `requireAdmin` خارج هذه الدوال — تعليقه صحيح والحدّ في
القاعدة. التفصيل كامل في «رسائل» أعلاه.

**بهذا انتهى ما أستطيع إغلاقه من هنا.** الباقي كلّه محجوز بأحد سببين:

| ما بقي | لماذا لا أستطيع إغلاقه |
|---|---|
| تطبيق الهجرات الأربع | حساب Supabase المتاح لي لا يملك مشروع سودجري — ولو طبّقتُ عمياً لأصبتُ مشروعاً آخر بجدول `service_providers` لا صلة له |
| سياسة `SELECT` على `storage.objects` (ص-1) | استعلام على الإنتاج |
| ~~سحب الدوال الخمس إلى المستودع (ع-4)~~ | **أُغلق 2026-09-03** — `20260903120000_document_dashboard_functions.sql`. صار الاتصالُ بالإنتاج متاحاً فسُحبت بـ`pg_get_functiondef` |
| إخفاء زر «اعتماد» عن مقدّم الخدمة · `paused_by_owner` · توحيد نسختَي `requireAdmin` القديمتين · تطبيق الجوّال | **للبنّاء** — واجهة ومنتج، لا تصحيح |

### 2026-08-20 · الجولة الثالثة · المصحّح

**أخذتُ د-3 بدل تركها للبنّاء** — كانت في تقرير التسليم ضمن «خذ منه ما تشاء»، ولم يطالب
بها أحد في الجدول، فأنجزتها. عدّلتُ التسليم ليعكس ذلك حتى لا يبدأها أحد بعدي.

| ما أُصلح | أين |
|---|---|
| `admin/feedback` و`admin/providers` (٥ دوال) كانت بلا أي فحص صلاحية في التطبيق — أُضيف `requireAdmin` مشترك | `lib/adminGuard.ts` + الملفان |
| الدوال الخمس صارت تتحقق أن صفّاً تأثّر فعلاً بـ `.select("id")` | نفس الملفين |
| رسائل المشغّلات العربية تُعرض، وما عداها يُسجَّل ويُستبدل بنص ثابت (`error.code === 'P0001'`) | `admin/providers/actions.ts` |

**فُحص ولم يُصلح — نتيجتان جديدتان في التقرير (§٧):**
- **ص-1:** مخزن `evidence` وسياساته خارج المستودع. **تحقّقنا من الإنتاج: `public = false`**
  — فالمخزن خاص وأسوأ الاحتمالات مستبعد. **وطبقة التطبيق نظيفة أيضاً:** لا مسار في
  الكود يعرض أدلة مزارع لآخر — الملف العام يُرجع تجميعات لا ملفات. بقي الخطر في تجاوز
  التطبيق: نداء مباشر لواجهة التخزين، وتحكمه سياسة `SELECT` على `storage.objects` وحدها.
- **ص-2:** شرط «وجود دليل» يتحقق من وجود صفّ لا من وجود ملف.

**ملاحظة:** بقي نسختان من `requireAdmin` في `admin/actions.ts` و`admin/review/actions.ts`
بسلوكين مختلفين (إعادة توجيه مقابل إرجاع `null`). لم أوحّدهما لتقليل مساحة التعارض.

### 2026-08-20 · الجولة الثانية من الإصلاح · المصحّح

**الفرع:** `claude/project-review-analysis-dt7lsy` · `lint` و`build` نظيفان.

| ما أُصلح | أين |
|---|---|
| `confirmInvestment` كان يتجاهل نتيجة الـ RPC — أي رفض يبدو نجاحاً (د-4) | `admin/actions.ts` |
| `updateProjectStatus` صار يتحقق أن صفّاً تغيّر فعلاً (د-3) | `admin/actions.ts` |
| **قيد دفتر الحسابات** كان يفشل بصمت (د-4) | `seasons/actions.ts` |
| `completeSeason` صار يتحقق من التنفيذ (د-3) | `seasons/actions.ts` |
| التراجع الفاشل بعد فشل جزئي صار يُسجَّل بدل ترك سجل يتيم (د-4) | `contracts` · `herds` · `seasons` |
| رسائل PostgREST الخام لم تعد تصل للمتصفح (د-5) | `admin/actions.ts` |
| جدول `assistant_requests` دخل المستودع (ع-4 جزئياً) | `20260820050000_assistant_requests_table.sql` |

**باقٍ:** ع-4: أربع دوال ما زالت خارج المستودع — وأضيف إليها الآن مخزن `evidence` (ص-1).
> **لاحقاً (2026-09-03):** الأربعُ خرجت — بل خمسٌ، إذ كشف بحثٌ متعدّدُ الأسطر خامسةً
> (`match_knowledge_entries`) فاتَت العدَّ الأوّل. وص-1 حُسم بفحص الإنتاج: المخزن خاصّ
> وسياستُه `foldername[1] = auth.uid() or is_admin()`.

### 2026-08-19 · الجولة الأولى من الإصلاح · المصحّح

| ما أُصلح | أين |
|---|---|
| اعتماد المرحلة صار من صلاحية العميل أو الإدارة، لا مقدّم الخدمة (د-1) | `20260819180000_milestone_approval_actor.sql` |
| `active` انضم لحراسة التوثيق، فلا تُعيد الجهة الموقوفة تفعيل نفسها (د-2) | `20260819180100_guard_provider_active.sql` |
| سحب `EXECUTE` عن `check_assistant_rate_limit` + قلب الافتراض للدوال الجديدة (ع-1، ع-2) | `20260819180200_rate_limit_grants.sql` |
| تحديد المعدّل صار عبر `service_role` **ويفشل مغلقاً** (ع-3) | `assistantRateLimit.ts` + `api/assistant/route.ts` |
| مفتاح Gemini انتقل إلى ترويسة `x-goog-api-key` (م-2) | `engines.ts`, `embedding.ts` (٣ مواضع) |
| حذف حقل السعر المخفي الميت من نموذج الاستثمار (م-3) | `projects/[slug]/page.tsx` |

**⚠️ ترتيب النشر إلزامي:** الهجرات أولاً ثم الكود. وتحديداً `20260819180200` والتغيير في
مسار المساعد **يجب أن ينزلا معاً** — الملف يشرح السبب في رأسه.

### 2026-08-19 · مراجعة أمنية على `0514ac1` · المصحّح

**التقرير:** [`security-review-2026-08-19.md`](security-review-2026-08-19.md)

**فُحص:** الهجرات العشرون · مسارات API الخمسة · مسار المال · نموذج الأدوار · كل دوال
الخادم الـ41 في 16 ملفاً.

**لم يُفحص:** رفع الملفات وبيانات EXIF · تطبيق الجوّال (79 ملف Flutter) · وحدات الخدمات
والثروة الحيوانية و FAOSTAT ودراسة القناة كمنطق أعمال.

### 2026-08-12 → 08-19 · مراجعة أولى — أُلغيت · المصحّح

بُنيت على `8388259`، وتبيّن أنها متأخرة 97 commit. كل ما أصلحته كان مُصلَحاً على `main`
بالفعل، فأُلغيت واستُبدلت بالمراجعة أعلاه. الـ commits محفوظة عند `c0ce1dd` إن احتيج إليها.

**الدرس المستخلص:** هذا الملف.
