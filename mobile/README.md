# تطبيق سودجري

تطبيق فلوتر لمنصة سودجري، يعمل على نفس قاعدة البيانات ونفس المساعد الذي يخدم الموقع.

## ما فيه

| الشاشة | يحتاج اتصالاً؟ |
|---|---|
| **الاحتياج المائي** — حساب FAO-56 كامل، وتفصيل شهري، وأعلى شهر طلباً | **لا** |
| **مساعد سودجري** — محادثة عربية عبر `/api/assistant` | نعم |
| **قاعدة المعرفة** — قراءة المُدخلات مباشرة مع بحث | نعم |

حاسبة الري تعمل بلا شبكة عن قصد. التغطية في المشاريع المروية غير موثوقة، وأداة لا تعمل إلا حيث توجد شبكة ليست أداة لمن يحتاجها.

## التشغيل

الإعدادات تُمرَّر وقت البناء عبر `--dart-define`. لا يوجد فيها سرّ: مفتاح `anon` مُعدّ للنشر، وكل جدول يصله محميّ بسياسات الصفوف على الخادم.

```bash
flutter run \
  --dart-define=SUPABASE_URL=https://<ref>.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=<anon> \
  --dart-define=SITE_URL=https://<موقعك>
```

تشغيلٌ بلا هذه القيم يعمل، لكن شاشتَي المعرفة والمساعد ستقولان إن التطبيق غير مضبوط — وحاسبة الري تظلّ كاملة.

## الفحص

```bash
flutter analyze --fatal-infos
flutter test
```

`test/agronomy_test.dart` ليس فحصاً عادياً. التطبيق يحمل **نسخته الخاصة** من محرك FAO-56 (`lib/agronomy.dart`) لأنه يجب أن يعمل بلا شبكة، وثمن ذلك أن النسختين قد تفترقان بصمت فيُعطي الموقع رقماً والتطبيق رقماً آخر.

فالفحص يثبّت مخرجات Dart على أرقام أنتجها محرك TypeScript في `src/lib/agronomy.ts`. **أي تغيير في أيّ من الطرفين يُحرّك رقماً يُسقط البناء** — فإما تُحدَّث النسخة الأخرى، أو يُشرح لماذا يجب أن تختلفا.

لتحديث الأرقام المرجعية بعد تغيير مقصود: شغّل محرك الويب على الحالات الست نفسها والصق مخرجاته مكان القيم في أعلى ملف الفحص.

## البناء

`.github/workflows/mobile.yml` يبني APK قابلاً للتثبيت ويرفعه كملف في نتيجة التشغيل. اضبط أولاً في **Settings ← Secrets and variables ← Actions ← Variables**:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SITE_URL`

البناء **تجريبي (debug)**. النشر في متجر يحتاج مفتاح توقيع، وهو سرّ يخصّ مالك الإدراج لا هذا المستودع.

## ما ليس فيه بعد

تسجيل الأرض وتوثيق الموسم ورفع الأدلة — تبقى في الويب حالياً. فيها رفع ملفات وطوابير مراجعة تستحق تصميماً خاصاً بالهاتف لا نقلاً مضغوطاً.
